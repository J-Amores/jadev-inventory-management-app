'use client';

// Inventory table (Client Component)
export default function InventoryList() {
  // TODO: Fetch items from /api/inventory on mount
  // TODO: Delete: fetch(`/api/inventory/${id}`, { method: 'DELETE' })
  // TODO: Refresh list after operations

  return (
    <div>
      <h2>Inventory List</h2>
      {/* Search/filter table with CRUD operations */}
    </div>
  );
}
